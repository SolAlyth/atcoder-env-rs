[package]
name = "atcoder"
version = "0.1.0"
edition = "2021"
default-run = "main"

[workspace]
# cargo ... -p mylib
members = ["mylib"]

[dependencies]
mylib = { path = "mylib" }
ac-library-rs = "0.1.1"

[package.metadata.docs.rs]
rustdoc-args = [
    "--html-in-header",
    "katex.html",
]
